import React from 'react';
import {Text} from 'react-native';
import {styles} from '@/styles/components/TodoHeader.styles';

export const TodoHeader = () => {
  return <Text style={styles.title}>My Tasks</Text>;
};
